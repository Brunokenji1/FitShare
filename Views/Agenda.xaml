<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppFitShare.Views.Agenda"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:AppFitShare.Converters"
    xmlns:models="clr-namespace:AppFitShare.Models"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodels="clr-namespace:AppFitShare.ViewModels"
    Title="Agenda"
    Background="#0c1420"
    IconImageSource="agenda.png"
    NavigationPage.HasNavigationBar="False">
    <VerticalStackLayout>
        <Grid Background="#003918" ColumnDefinitions="*,Auto">
            <Label
                Padding="30,15,100,20"
                BackgroundColor="#003918"
                FontAttributes="Bold"
                FontSize="20"
                Text="Lembrete"
                TextColor="White" />

            <ImageButton
                Grid.Column="1"
                Margin="4,4,10,4"
                Padding="2"
                Clicked="BtnAdicionarLembrete"
                HeightRequest="32"
                HorizontalOptions="Center"
                Source="plus_green.png"
                VerticalOptions="Center"
                WidthRequest="32" />
        </Grid>
        <CollectionView x:Name="CollectionViewLembretes">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout>
                        <Border
                            Margin="10"
                            Padding="10,20"
                            BackgroundColor="Transparent"
                            Stroke="#01b853"
                            StrokeShape="RoundRectangle 10"
                            StrokeThickness="2">

                            <Grid ColumnDefinitions="Auto, *" ColumnSpacing="15">

                                <Label
                                    Grid.Column="0"
                                    FontAttributes="Bold"
                                    FontSize="24"
                                    Text="{Binding HorarioFormatado}"
                                    TextColor="White"
                                    VerticalOptions="Center" />

                                <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                    <Label
                                        FontAttributes="Bold"
                                        FontSize="16"
                                        Text="{Binding Titulo}"
                                        TextColor="White" />

                                    <Label
                                        FontSize="12"
                                        Text="{Binding Descricao}"
                                        TextColor="#cccccc" />

                                    <Label
                                        FontAttributes="Italic"
                                        FontSize="10"
                                        Text="{Binding DiasResumo}"
                                        TextColor="#01b853" />
                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>